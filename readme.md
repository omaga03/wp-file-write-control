# 🔐 WP File Write Control (Security Dashboard)

**Version:** 7.0.5  
**Author:** IT Admin + RDI Omaga  
**Requires at least:** WordPress 5.0  
**Tested up to:** WordPress 6.x  
**Requires PHP:** 7.2+

---

## 📖 รายละเอียด (Description)

**WP File Write Control** คือปลั๊กอินด้านความปลอดภัยสำหรับ WordPress ที่ออกแบบมาเพื่อ "ล็อก" สิทธิ์การเขียนไฟล์ (File Write Permissions) ของโฟลเดอร์สำคัญๆ บนเซิร์ฟเวอร์ ได้แก่ **Uploads**, **Plugins** และ **Themes** เพื่อป้องกันไม่ให้มัลแวร์หรือแฮกเกอร์สามารถฝังไฟล์อันตราย (Backdoor/Web Shell) ลงในเว็บไซต์ของคุณได้

ปลั๊กอินนี้ช่วยให้คุณสามารถทำงานในโหมด **"Read-Only"** (อ่านได้อย่างเดียว) เป็นค่าเริ่มต้น และเปิดสิทธิ์เขียนไฟล์ **"ชั่วคราว"** เฉพาะตอนที่คุณต้องการอัปโหลดไฟล์ หรืออัปเดตปลั๊กอินเท่านั้น โดยระบบจะทำการปิดสิทธิ์กลับคืนให้อัตโนมัติเมื่อครบกำหนดเวลา

---

## ✨ ฟีเจอร์หลัก (Key Features)

1.  **🔒 ควบคุมสิทธิ์ไฟล์ (File Permission Control):**
    *   เปิด/ปิด สิทธิ์การเขียนไฟล์สำหรับโฟลเดอร์ `wp-content/uploads`
    *   เปิด/ปิด สิทธิ์การเขียนไฟล์สำหรับโฟลเดอร์ `wp-content/plugins`
    *   เปิด/ปิด สิทธิ์การเขียนไฟล์สำหรับโฟลเดอร์ `wp-content/themes`
    *   เปลี่ยน Permission ระหว่าง `775` (เขียนได้) และ `555` (อ่านได้อย่างเดียว)

2.  **⏱️ ระบบปิดอัตโนมัติ (Auto-Disable Timer):**
    *   เมื่อคุณกด "เปิด" สิทธิ์ ระบบจะนับถอยหลังและทำการ "ปิด" สิทธิ์ให้อัตโนมัติเมื่อครบเวลาที่กำหนด (ค่าเริ่มต้น 10 นาที) ป้องกันการลืมปิดสิทธิ์ทิ้งไว้

3.  **⚡ Turbo Mode (High Performance):**
    *   รองรับการเปลี่ยนสิทธิ์ไฟล์จำนวนมหาศาลได้อย่างรวดเร็วโดยใช้คำสั่ง System Command (`exec`) หากเซิร์ฟเวอร์รองรับ ลดปัญหา Timeout เมื่อมีไฟล์เยอะๆ

4.  **🛡️ API Security Protection:**
    *   ปกป้อง REST API Endpoint `/wp/v2/media`
    *   ต้องใช้ **Secret Key** (`X-WFWC-SECRET`) ในการอัปโหลดไฟล์ผ่าน API
    *   ระบบ **IP Whitelist** อนุญาตเฉพาะ IP ที่กำหนด
    *   ระบบ **Auto Ban** บล็อก IP อัตโนมัติหากใส่ Key ผิดเกิน 5 ครั้ง
    *   ตรวจสอบนามสกุลไฟล์ที่อนุญาต (jpg, jpeg, png, gif, webp, pdf)

5.  **💻 User Interface ที่ใช้งานง่าย:**
    *   **Dashboard Widget:** ดูสถานะและควบคุมได้จากหน้าแรกของ Admin
    *   **Admin Bar Notice:** แถบแจ้งเตือนสถานะเมื่อเข้าไปในหน้า Media, Plugins หรือ Themes
    *   **Post Editor Meta Box:** ปุ่มเปิด/ปิดสิทธิ์อัปโหลดรูปภาพโดยตรงในหน้าเขียนบทความ (Post/Page) ไม่ต้องสลับหน้าไปมา

6.  **📜 Activity Logs & Audit:**
    *   บันทึกประวัติการใช้งาน (ใคร, ทำอะไร, เมื่อไหร่, IP ไหน)
    *   ตรวจสอบสถานะไฟล์สำคัญๆ (wp-config.php, .htaccess) ว่าปลอดภัยหรือไม่

7.  **🔔 Email Notifications:**
    *   แจ้งเตือนทางอีเมลเมื่อมีการเปลี่ยนแปลงสถานะความปลอดภัย (เปิดสิทธิ์)

8.  **🧱 Server Hardening:**
    *   สร้างไฟล์ `.htaccess` ในโฟลเดอร์ Uploads เพื่อป้องกันการรันไฟล์ PHP (ป้องกันการฝัง Script)

---

## 🛠️ การติดตั้ง (Installation)

1.  อัปโหลดโฟลเดอร์ `wp-file-write-control` ไปที่ `Directory /wp-content/plugins/`
2.  เปิดใช้งานปลั๊กอินผ่านเมนู **Plugins** ใน WordPress
3.  ไปที่เมนู **File Write Control** เพื่อตั้งค่าเริ่มต้น

---

## 🖥️ วิธีการใช้งาน (Usage)

### 1. การควบคุมทั่วไป (General Control)
*   ไปที่เมนู **File Write Control**
*   คุณจะเห็นการ์ด 3 ใบ: **Uploads**, **Plugins**, **Themes**
*   สถานะสีแดง 🟥 = **ปิด (Locked)**: ปลอดภัย เขียนไฟล์ไม่ได้
*   สถานะสีเขียว 🟩 = **เปิด (Allowed)**: เขียนไฟล์ได้ (มีความเสี่ยง)
*   คลิกปุ่ม **"🔓 เปิด ... นาที"** เพื่อเปิดสิทธิ์ชั่วคราว
*   คลิกปุ่ม **"🔒 ปิดทันที"** เพื่อล็อกสิทธิ์ทันที

### 2. การอัปโหลดรูปภาพในบทความ (Post Editor)
*   ในหน้าเขียนบทความ (Post/Page) จะมีกล่อง **File Write Control** อยู่ด้านขวา (หรือด้านบนของ Featured Image)
*   หากต้องการอัปโหลดรูป ให้กดปุ่ม **"🔓 เปิด"** รอสักครู่จนเป็นสีเขียว แล้วจึงทำการอัปโหลดรูปภาพ
*   เมื่อเสร็จแล้ว ระบบจะปิดเองตามเวลา หรือกดปิดเองก็ได้

### 3. การตั้งค่า (Settings)
*   **Enable Email Notification:** เปิดเพื่อรับอีเมลแจ้งเตือนเมื่อมีการเปิดสิทธิ์
*   **Auto-Disable Timeout:** ตั้งเวลา (นาที) ที่จะให้ระบบปิดสิทธิ์อัตโนมัติ
*   **Enable API Access:** เปิดใช้งานระบบป้องกัน API
    *   **Secret Key:** คีย์ลับที่ต้องแนบมากับ Header `X-WFWC-SECRET`
    *   **Allowed IPs:** รายการ IP ที่อนุญาตให้ยิง API (บรรทัดละ 1 IP)

---

## 🔌 API Documentation (สำหรับนักพัฒนา)

หากต้องการอัปโหลดไฟล์ผ่าน REST API ในขณะที่ปลั๊กอินทำงานอยู่ ต้องส่ง Header ดังนี้:

**Endpoint:** `POST /wp-json/wp/v2/media`

**Headers:**
```http
Authorization: Bearer <token>
Content-Disposition: attachment; filename="image.jpg"
X-WFWC-SECRET: <your_secret_key>
```

*   หากไม่ส่ง Key หรือ Key ผิด จะได้รับ `403 Forbidden`
*   หาก IP ถูกแบน จะต้องรอ 1 ชั่วโมง

---

## ⚠️ ข้อควรระวัง (Important Notes)

*   **System Requirements:** ปลั๊กอินนี้ต้องการให้ PHP สามารถสั่ง `chmod` หรือ `exec` ได้ หากโฮสต์ปิดฟังก์ชันเหล่านี้ ปลั๊กอินอาจทำงานได้ไม่สมบูรณ์
*   **Windows Server:** บน Windows การจัดการ Permission อาจแตกต่างจาก Linux ระบบจะพยายามใช้ `icacls` หรือฟังก์ชัน PHP มาตรฐาน แต่อาจไม่ครอบคลุมเท่า Linux
*   **Do not delete:** ห้ามลบไฟล์ `wp-file-write-control-backup-ห้ามลบ ห้ามเปลี่ยน.php` เนื่องจากเป็นไฟล์สำรองฉุกเฉิน

---

**© 2025 IT Admin + RDI Omaga**
