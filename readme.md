# 🔐 WP File Write Control (Security Dashboard)

**Version:** 7.0.7  
**Author:** IT Admin + RDI Omaga  
**Requires at least:** WordPress 5.0  
**Tested up to:** WordPress 6.x  
**Requires PHP:** 7.2+

---

## 📖 รายละเอียด (Description)

**WP File Write Control** คือปลั๊กอินรักษาความปลอดภัยระดับสูงสำหรับ WordPress ที่ช่วย "ล็อก" ระบบไฟล์ของคุณให้เป็น **Read-Only** เพื่อป้องกันการถูกฝังมัลแวร์, Web Shell หรือการแก้ไขไฟล์โดยไม่ได้รับอนุญาต

จุดเด่นของเวอร์ชันนี้คือระบบ **Dual Mode Protection** ที่ให้คุณเลือกได้ว่าจะล็อกเฉพาะโฟลเดอร์ (ทำงานเร็ว) หรือล็อกลึกถึงระดับไฟล์ทุกไฟล์ (ปลอดภัยสูงสุด) พร้อมระบบ **Real-Time Verification** ที่ทดสอบการเขียนไฟล์จริงๆ เพื่อความแม่นยำ 100%

---

## ✨ ฟีเจอร์หลัก (Key Features)

### 1. 🛡️ ระบบควบคุมสิทธิ์ (Permission Control)
*   **Uploads Control:** ควบคุมโฟลเดอร์ `wp-content/uploads`
*   **Plugins Control:** ควบคุมโฟลเดอร์ `wp-content/plugins`
*   **Themes Control:** ควบคุมโฟลเดอร์ `wp-content/themes`
*   **Smart Auto-Disable:** ระบบจำค่าโหมดที่คุณเปิดใช้งาน และจะปิดคืนให้ด้วยโหมดเดิมอัตโนมัติเมื่อครบเวลา

### 2. 🚀 โหมดการทำงาน (Operation Modes)
*   **⚡ Turbo Mode (Default):** เปลี่ยน Permission เฉพาะ "โฟลเดอร์" เท่านั้น ทำงานรวดเร็วมาก เหมาะสำหรับเว็บไซต์ที่มีไฟล์จำนวนมหาศาล
*   **🐢 Deep Mode (Folder + File):** (เฉพาะ Uploads) เปลี่ยน Permission ของ "ทุกไฟล์และทุกโฟลเดอร์ย่อย" แบบ Recursive เหมาะสำหรับต้องการความปลอดภัยสูงสุด (อาจใช้เวลานานหากมีไฟล์เยอะ)

### 3. ✅ ความแม่นยำสูง (High Precision)
*   **Real Write Test:** ไม่ได้เช็คแค่ค่า Permission (775/555) แต่มีการทดลองสร้างไฟล์จำลอง (`.wfwc_check.tmp`) เพื่อตรวจสอบว่า PHP สามารถเขียนไฟล์ลง Disk ได้จริงหรือไม่ ตัดปัญหา Status หลอกตา

### 4. 🔒 API Security Fortress
*   **Secret Key Protection:** ต้องใช้ Header `X-WFWC-SECRET` ในการยิง API
*   **IP Whitelist:** กำหนด IP ที่อนุญาตให้ใช้งาน API ได้
*   **Auto Ban System:** หากใส่ Key ผิดเกิน 5 ครั้ง ระบบจะแบน IP นั้นทันที 1 ชั่วโมง
*   **Safe File Check:** ตรวจสอบนามสกุลไฟล์และ Content-Disposition อย่างละเอียด

### 5. 👮 Server Hardening
*   **Anti-Execution:** สร้างไฟล์ `.htaccess` ในโฟลเดอร์ Uploads เพื่อป้องกันการรันไฟล์ PHP (Block PHP Execution) ช่วยกันมัลแวร์ทำงาน

---

## 🛠️ การติดตั้ง (Installation)

1.  อัปโหลดโฟลเดอร์ `wp-file-write-control` ไปที่ `/wp-content/plugins/`
2.  เปิดใช้งานปลั๊กอินผ่านเมนู **Plugins**
3.  ไปที่เมนู **File Write Control** เพื่อเริ่มใช้งาน

---

## 🖥️ คู่มือการใช้งาน (User Guide)

### 1. หน้าควบคุมหลัก (Control Panel)
*   **สถานะสีแดง (Locked):** ปลอดภัย (เขียนไฟล์ไม่ได้)
*   **สถานะสีเขียว (Allowed):** มีความเสี่ยง (เขียนไฟล์ได้)
*   **Checkbox "🐢 Folder+File":**
    *   ⬜ **ไม่ติ๊ก:** ทำงานแบบ **Turbo Mode** (แนะนำทั่วไป)
    *   ✅ **ติ๊ก:** ทำงานแบบ **Deep Mode** (เปลี่ยนสิทธิ์ทุกไฟล์ย่อย)
*   **ปุ่มเปิด/ปิด:** กดเพื่อเปลี่ยนสถานะ หากเปิดอยู่จะมีตัวนับเวลาถอยหลัง

### 2. การใช้งานในหน้าเขียนบทความ (Post Editor)
*   จะมีกล่อง **File Write Control** อยู่เหนือรูปภาพ Featured Image หรือด้านขวา
*   สามารถกด **"🔓 เปิด"** เพื่ออัปโหลดรูปได้ทันทีโดยไม่ต้องออกจากหน้าเขียนบทความ
*   เมื่ออัปโหลดเสร็จ สามารถกด **"🔒 ปิด"** หรือปล่อยให้ระบบปิดเองก็ได้

### 3. การตั้งค่า (Settings)
*   **Enable Email Notification:** แจ้งเตือนทางอีเมลเมื่อมีการ "เปิด" สิทธิ์ (ไม่แจ้งตอนปิด เพื่อลดความรำคาญ)
*   **Auto-Disable Timeout:** ตั้งเวลาปิดอัตโนมัติ (หน่วย: นาที)
*   **Enable API Access:** เปิดระบบ API Security

---

## 🔌 API Documentation (สำหรับนักพัฒนา)

หากต้องการอัปโหลดไฟล์ผ่าน REST API (`POST /wp-json/wp/v2/media`) ต้องตั้งค่า Header ดังนี้:

### Headers Required
```http
Authorization: Bearer <your_token>
Content-Disposition: attachment; filename="image.jpg"
X-WFWC-SECRET: <your_secret_key>
```

### Response Codes
*   `201 Created`: อัปโหลดสำเร็จ
*   `403 Forbidden`:
    *   `wfwc_banned`: IP ของคุณถูกแบนชั่วคราว (รอ 1 ชม.)
    *   `rest_forbidden`: Key ไม่ถูกต้อง หรือไม่มีสิทธิ์

---

## ❓ ปัญหาที่พบบ่อย (Troubleshooting)

**Q: กดเปิดแล้วหมุนติ้วๆ ไม่หยุด (Timeout)?**  
A: หากคุณเลือกโหมด **"🐢 Folder+File"** และมีรูปภาพเป็นแสนรูป อาจทำให้ PHP ทำงานไม่ทัน (Timeout) แนะนำให้ใช้โหมดปกติ (ไม่ติ๊ก Checkbox) ก็เพียงพอต่อความปลอดภัยแล้ว

**Q: ลืมปิดสิทธิ์จะเป็นอะไรไหม?**  
A: ไม่เป็นไร ระบบมี **Auto-Disable** (Cron Job) ที่จะคอยตรวจสอบทุกนาที และจะปิดสิทธิ์ให้เองเมื่อครบเวลาที่ตั้งไว้

**Q: เข้าหน้าเว็บไม่ได้ / พังยับเยิน ทำไงดี?**  
A: หากเกิดข้อผิดพลาดร้ายแรง ให้เข้าผ่าน FTP หรือ File Manager ของโฮสต์ แล้วเปลี่ยนชื่อโฟลเดอร์ปลั๊กอินเป็น `wp-file-write-control-disabled` ระบบจะหยุดทำงานทันที

---

**© 2025 IT Admin + RDI Omaga**
